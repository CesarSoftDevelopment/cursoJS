/**
 * <head> <body> filho de <html>    <head> é irmão de <body>
 * =======================================================
 * Ambos estão no mesmo nível(body, html)
 * ======================================
 * <title> é filho do <head>
 * ========================
 * <title>Olá mundo</title> olá mundo é filho de <title>
 * ====================================================
 * body -> possui vários filhos(h1, a, p...)
 * ==========================================
 * href não é um elemento| href é atributo do ancora(a)
 * ====================================================
 * Chamamos cada elemento(html, body, h1, a) de nó da página
 * texto dentro do ancora é filho de ancora
 * Pai é quando o elemento tem outros elementos dentro dele
 * Filho é quando este elemento está dentro de um elemento

A GRANDE JOGADA DO JAVASCRIPT É MANIPULAR A DOM OU SEJA, MANIPULAR TODAS AS TAGS DE ACORDO COM
A NOSSA NECESSIDADE, EXCLUI, INCLUI, MOVE, TUDO QUE QUISERMOS FAZER COM O JAVASCRIPT!!!
 */

/**
 * PARA MANIPULAR A DOM, UTILIZAMOS  A API DOM
 * 
 */

